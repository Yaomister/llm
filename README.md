# llm

A small, from-scratch language model project inspired by the GPT-2 paper, with custom modifications and improvements.

## Overview

This repository contains the building blocks for a transformer-based language model implemented in PyTorch. The design is based on GPT-2 but is being extended with experimental improvements and a simplified training pipeline.
